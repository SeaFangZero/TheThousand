{
  "name": "the-thousand",
  "version": "1.0.0",
  "description": "Anonymous race-to-1000-subscribers leaderboard",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx netlify dev",
    "deploy": "npx netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
